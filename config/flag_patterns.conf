# StegoForge Flag Patterns
# Each line is a regex pattern. Lines starting with # are ignored.

picoCTF\{[^}]+\}
NCSE\{[^}]+\}
FLAG\{[^}]+\}
CTF\{[^}]+\}
flag\{[^}]+\}
[A-Z]{3,10}\{[^}]+\}
[a-z]{3,15}\{[^}]+\}
