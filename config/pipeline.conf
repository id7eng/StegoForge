# Pipeline Rules: emitted_type -> module_name
# When a module emits data of the specified type, save it as a temp file
# and run the target module on that extracted data.
#
# Format: emitted_type|module_name
base64_string|flag_scanner
keyword|flag_scanner
flag|flag_scanner
